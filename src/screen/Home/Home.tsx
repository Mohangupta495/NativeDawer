import { StyleSheet, Text, TouchableOpacity, View } from 'react-native'
import React from 'react'
import strings from '../../rn-localize/language'

const Home = (props:any) => {
  return (
    <View>
      <Text>Home</Text>
      {/* <TouchableOpacity><Text style={globalStyles.fontBlack}>Click Me</Text></TouchableOpacity> */}
    </View>
  )
}

export default Home

const styles = StyleSheet.create({})